<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Message</title>
  <link rel="stylesheet" href="/newmessage.css">
</head>
<body>
  <div class="page-container">
    <% if (user) { %>
      <div class="form-wrapper">
        <h1 class="page-title">📝 Create a New Message</h1>

        <form action="/message/<%=msg_id%>/edit" method="POST" class="message-form">
          <div class="form-group">
            <label for="title">Title</label>
            <input 
              type="text" 
              id="title" 
              name="title" 
              value="<%=title %>"
              placeholder="Enter a catchy title..." 
              required
            >
          </div>

          <div class="form-group">
            <label for="message">Message</label>
            <textarea 
              
              id="message" 
              name="message" 
              rows="6" 
              placeholder="Write your message here..." 
              required
            ><%=message %></textarea>
          </div>

          <button type="submit" class="btn-submit">🚀 Post Message</button>
        </form>
      </div>
    <% } else { %>
      <div class="locked-wrapper">
        <h1 class="locked">🔒 You must be logged in to create a message.</h1>
        <a href="/login" class="btn-login">Log In</a>
      </div>
    <% } %>
  </div>
</body>
</html>
